{
  "name": "shati-flight-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test \"tests/*.test.js\"",
    "load-test": "node scripts/load-test.mjs --dry-run",
    "dev:command-center": "npm --prefix apps/command-center run dev",
    "build:command-center": "npm --prefix apps/command-center run build",
    "test:command-center": "npm --prefix apps/command-center run test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
