import { Input } from "../ui/input";

export function FilterBar({ filters, onChange, projects, teams, users }: { filters: Record<string, string>; onChange: (key: string, value: string) => void; projects: Array<{ id: string; name: string }>; teams: Array<{ id: string; name: string }>; users: Array<{ id: string; displayName: string }> }) {
  const select = (label: string, key: string, options: Array<{ id: string; label: string }>, empty: string) => <select aria-label={label} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm" value={filters[key] ?? ""} onChange={(event) => onChange(key, event.target.value)}><option value="">{empty}</option>{options.map((option) => <option key={option.id} value={option.id}>{option.label}</option>)}</select>;
  return <div className="flex flex-wrap items-center gap-2"><Input aria-label="جست‌وجوی فعالیت" placeholder="جست‌وجوی فعالیت…" value={filters.q ?? ""} onChange={(event) => onChange("q", event.target.value)} />{select("پروژه", "projectId", projects.map((item) => ({ id: item.id, label: item.name })), "همه پروژه‌ها")}{select("تیم", "teamId", teams.map((item) => ({ id: item.id, label: item.name })), "همه تیم‌ها")}{select("مسئول", "assigneeId", users.map((item) => ({ id: item.id, label: item.displayName })), "همه افراد")}<select aria-label="فاز" className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm" value={filters.phase ?? ""} onChange={(event) => onChange("phase", event.target.value)}><option value="">همه فازها</option><option value="PRODUCT">محصول</option><option value="DESIGN">طراحی</option><option value="DEVELOPMENT">توسعه</option><option value="DELIVERY">تحویل</option></select><select aria-label="وضعیت" className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm" value={filters.status ?? ""} onChange={(event) => onChange("status", event.target.value)}><option value="">همه وضعیت‌ها</option><option value="TODO">در صف</option><option value="IN_PROGRESS">در حال انجام</option><option value="BLOCKED">مسدود</option><option value="DONE">تمام‌شده</option></select><select aria-label="بازه زمانی" className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm" value={filters.zoom ?? "day"} onChange={(event) => onChange("zoom", event.target.value)}><option value="day">روزانه</option><option value="week">هفتگی</option><option value="month">ماهانه</option></select></div>;
}
