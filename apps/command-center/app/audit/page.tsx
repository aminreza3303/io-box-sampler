"use client";
import { useEffect, useState } from "react";
export default function AuditPage() { const [events, setEvents] = useState<any[]>([]); useEffect(() => { fetch("/api/audit").then((response) => response.json()).then(setEvents).catch(() => undefined); }, []); return <main className="min-h-screen bg-slate-50 p-6 text-slate-900 sm:p-10" dir="rtl"><div className="mx-auto max-w-6xl space-y-6"><p className="text-sm font-semibold text-cyan-600">مقر فرماندهی / ردپای تصمیم</p><h1 className="text-3xl font-black">گزارش فعالیت و ممیزی</h1><p className="text-sm text-slate-500">هر تغییر مهم، تصمیم و اجرای عامل با مالک، هدف و زمان قابل پیگیری است.</p><div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="w-full text-right text-sm"><thead className="bg-slate-50"><tr><th className="p-4">عملیات</th><th className="p-4">هدف</th><th className="p-4">زمان</th></tr></thead><tbody>{events.map((event) => <tr key={event.id} className="border-t border-slate-100"><td className="p-4 font-semibold">{event.action}</td><td className="p-4 text-slate-500">{event.targetType} / {event.targetId}</td><td className="p-4 text-slate-500">{new Date(event.createdAt).toLocaleString("fa-IR")}</td></tr>)}</tbody></table>{events.length === 0 && <p className="p-6 text-sm text-slate-500">هنوز رویدادی ثبت نشده است.</p>}</div></div></main>; }
